export const DRAWER_WIDTH = 320;

/**
 * Id of the drawer panel. Shared so the trigger that opens it (the navbar menu
 * button) can point `aria-controls` at the element it actually reveals.
 */
export const DRAWER_PANEL_ID = "app-drawer";
