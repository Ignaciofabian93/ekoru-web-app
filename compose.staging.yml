name: "ekoru-app-staging"

services:
  ekoru-app:
    image: ekoru-app-staging
    build:
      context: .
      dockerfile: Dockerfile
      # args:
      # NEXT_PUBLIC_ENVIRONMENT: staging
    restart: always
    container_name: ekoru-app-staging
    env_file:
      - .env.staging
    ports:
      - "3001:3000"
    networks:
      - ekoru-network
    volumes:
      - /home/ekoru/images:/app/images
    deploy:
      resources:
        limits:
          cpus: "1.0"
          memory: 512M
        reservations:
          cpus: "0.5"
          memory: 256M
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 60s
      timeout: 5s
      retries: 2
      start_period: 30s

networks:
  ekoru-network:
    external: true
