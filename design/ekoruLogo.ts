/**
 *
 * Usage:  className={ekoruLogoImageClass}
 * ─────────────────────────────────────────────────────────────────
 */

export const ekoruLogoLinkClass =
  "rounded-sm outline-none focus-visible:ring-2 focus-visible:ring-white/80";

/** Default sizing, used when the caller passes no className of its own. */
export const ekoruLogoImageClass = "min-w-18 w-full max-w-24";
